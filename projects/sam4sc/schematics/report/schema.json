{
  "$schema": "http://json-schema.org/schema",
  "$id": "SchematicsReport",
  "title": "Semi-Automatic Migrator for Standalone Components",
  "type": "object",
  "properties": {
    "project": {
      "type": "string",
      "description": "The name of the project you want to have reported",
      "$default": {
        "$source": "projectName"
      }
    }
  }
}
